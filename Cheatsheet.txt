git status                       	  # Check for changes in your repo
git add .                          	  # Stage all changes for commit
git commit -m "Your commit message"  	  # Commit changes
git pull origin main --rebase      	  # Sync latest code from GitHub
git push origin main             	  # Push changes to GitHub
git log --oneline               	  # View commit history (short format)
git checkout -b new-branch      	  # Create and switch to a new branch
git merge new-branch            	  # Merge a branch into the current branch
git reset --hard HEAD~1         	  # Undo last commit (be careful!)

node server.js                	   	  # Run your Node.js server
npm install                      	  # Install project dependencies
npm start                        	  # Start the app (if configured in package.json)
npm run dev                      	  # Start in development mode (if using nodemon)
npm update                       	  # Update all dependencies
npm restart                               # Restart the server (if configured in package.json)
rm -rf node_modules package-lock.json && npm install  		# Clean & reinstall dependencies

psql -h db.xqokfaqtktzbavqiusbb.supabase.co -U postgres -d postgres  	# Connect to Supabase PostgreSQL
\dt                            		  # List all tables
SELECT * FROM quotes;           	  # View all quotes in database
UPDATE quotes SET status='Accepted' WHERE id=1;  			# Update quote status

ls                               # List files in a directory
ls -la                           # List all files, including hidden ones
cd folder-name                   # Change directory
cd ..                            # Go up one directory
pwd                              # Print current working directory
mkdir new-folder                 # Create a new folder
touch newfile.txt                # Create a new empty file
rm filename.txt                   # Delete a file
rm -rf folder-name               # Delete a folder and all its contents

ps aux | grep node               # Check if Node.js is running
kill -9 <PID>                    # Kill a process by ID
top                              # Monitor system performance
htop                             # Advanced process monitor (if installed)

curl -X GET http://localhost:3000/admin/quotes   # Test API GET request
curl -X POST http://localhost:3000/get_transactions -d '{"access_token": "your_token"}' -H "Content-Type: application/json"  # Test API POST request
ping google.com                    # Check internet connectivity
netstat -tulnp | grep LISTEN        # Check which ports are in use

chmod +x script.sh                  # Make a script executable
chown user:group filename            # Change file ownership
sudo !!                              # Rerun last command as sudo
history                              # Show command history
clear                                # Clear terminal screen